<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Filesystem Disk
    |--------------------------------------------------------------------------
    |
    | Определяет файловое хранилище (disk) по умолчанию, которое будет использоваться
    | в приложении. Доступны как локальные диски, так и облачные.
    |
    */
    'default' => env('FILESYSTEM_DISK', 'local'),

    /*
    |--------------------------------------------------------------------------
    | Filesystem Disks
    |--------------------------------------------------------------------------
    |
    | Здесь можно настроить любое количество файловых "дисков". Можно создавать
    | несколько дисков с одним драйвером, например несколько S3-бакетов.
    |
    | Поддерживаемые драйверы: "local", "ftp", "sftp", "s3"
    |
    */
    'disks' => [

        'local' => [
            'driver' => 'local',                  // Локальное хранилище
            'root' => storage_path('app'),        // Корневая папка
            'throw' => false,                     // Не выбрасывать исключения при ошибках
        ],

        'public' => [
            'driver' => 'local',                  // Локальное хранилище для публичных файлов
            'root' => storage_path('app/public'), // Корневая папка
            'url' => env('APP_URL').'/storage',   // URL для доступа к файлам
            'visibility' => 'public',             // Публичная доступность
            'throw' => false,
        ],

        's3' => [
            'driver' => 's3',                     // Хранилище Amazon S3
            'key' => env('AWS_ACCESS_KEY_ID'),    // Ключ доступа AWS
            'secret' => env('AWS_SECRET_ACCESS_KEY'), // Секретный ключ AWS
            'region' => env('AWS_DEFAULT_REGION'),    // Регион AWS
            'bucket' => env('AWS_BUCKET'),            // Название бакета
            'url' => env('AWS_URL'),                  // URL бакета
            'endpoint' => env('AWS_ENDPOINT'),        // Пользовательский эндпоинт (опционально)
            'use_path_style_endpoint' => env('AWS_USE_PATH_STYLE_ENDPOINT', false), // Использовать path-style
            'throw' => false,
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | Symbolic Links
    |--------------------------------------------------------------------------
    |
    | Здесь можно указать символические ссылки, которые будут созданы
    | при выполнении команды `php artisan storage:link`. Ключи — это
    | путь ссылки, а значения — путь назначения.
    |
    */
    'links' => [
        public_path('storage') => storage_path('app/public'),
    ],

];
